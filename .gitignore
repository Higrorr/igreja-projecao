# Dados baixados (JSON das traduções) - regeneráveis pelo script
dados/

# Banco SQLite gerado em tempo de execução (criado na primeira execução)
database/

# __pycache__ do Python
__pycache__/
*.pyc

# Ambiente virtual
.venv/
venv/

# Env
.env

# Segredos (chave da YouTube Data API) — vão embutidas apenas no exe de build
backend/secrets.py

# Artefatos intermediários do PyInstaller (o igreja.exe é versionado; build/ e spec são intermediários)
build/